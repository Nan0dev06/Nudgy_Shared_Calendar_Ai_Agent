# Secrets — never commit
.env
backend/.tokens/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# SQLite (Phase 2+)
*.db
*.sqlite3

# Frontend
frontend/node_modules/

# Backups of .env made while editing it — same secrets, same rules.
.env.backup-*
.env.*.backup
